'use strict';

let numberOfToys = 2;//number type
console.log(`Number of toys = ${numberOfToys}`);// "`" is grave accent, sreing with additional functionality

let bigIntNumber = 1n;
console.log(typeof(bigIntNumber));
console.log(bigIntNumber);
